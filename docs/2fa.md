# 2fa

Interactive TOTP helper.

```bash
2fa
2fa -h
```

Prompts for a Base32 secret (hidden), copies a 6-digit TOTP to the clipboard, and prints a framed `Code copied: <code>` message.

Requires `node` and `pbcopy`.
