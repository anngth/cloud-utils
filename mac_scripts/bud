#!/bin/zsh
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd)"
if ! command -v python3 >/dev/null 2>&1; then
  echo "python3 is required to run bud" >&2
  exit 1
fi
exec python3 "$SCRIPT_DIR/functions/brew-desired-update/__main__.py" "$@"
