#!/bin/zsh
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd)"
if ! command -v node >/dev/null 2>&1; then
  print -P "%F{red}❌ node is required to run budj%f" >&2
  exit 1
fi
exec node "$SCRIPT_DIR/functions/brew-desired-update-js/cli.mjs" "$@"
